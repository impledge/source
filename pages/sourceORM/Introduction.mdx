# Overview (Coming soon...)

ORM's are heavy in nature and require strict control over schema. Here we aim to implement a lightweight solution using what we call `schema-opposite` protocol to handle your data models. This is currently in development!